import os
from setuptools import find_packages
from setuptools import setup

setup(
    name='ormuco_question_c',
    version='0.1',
    author='Gustavo Lepri',
    description='Geo Distributed LRU with expiration'
)
